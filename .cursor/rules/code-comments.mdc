---
description: 所有代码修改必须完善详细注释（参数、方法、类型、模块）
alwaysApply: true
---

# 代码注释规范（强制）

每次新增或修改代码时，**必须同步完善注释**，不得只改逻辑不补注释。

## 适用范围

- C#（`desktop/src/**`）：XML 文档注释 `///`
- TypeScript / Vue（`desktop/ui/src/**`）：JSDoc / 文件头说明
- 公共 API、内部方法、关键、组件 props、关键事件回调、复杂分支均需注释

## 必须写清的内容

1. **文件 / 模块**：职责、在整体架构中的位置
2. **类型 / 类 / 组件**：用途、生命周期或使用场景
3. **方法 / 函数**：做什么、何时调用、副作用（IO / IPC / UI）
4. **参数**：含义、单位、可空约定、合法范围或示例
5. **返回值**：含义；失败时返回什么（`null` / 抛错 / 错误码）
6. **事件 / 消息 / IPC**：字段含义、方向（UI→Host / Host→UI）
7. **非显然逻辑**：为什么这样写（协议限制、路径约定、兼容旧 CLI 等）

## 语言

- 注释默认使用**简体中文**（与产品文档一致）
- 标识符保持英文；注释解释业务语义

## 禁止

- 无信息量注释（如 `// 赋值`、`// 返回`）
- 改了签名或行为却不更新旧注释
- 只写「见某某」而无本地摘要（可引用，但本处仍需简短说明）

## 检查清单（改完自问）

- [ ] 新增/改动的 public / internal 成员是否都有完整注释？
- [ ] 参数与返回值是否写全？
- [ ] 行为变化是否已反映在注释里？
