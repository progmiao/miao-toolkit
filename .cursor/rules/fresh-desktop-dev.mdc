---
description: 全新桌面开发：cli 仅作功能参考，禁止过渡方案
alwaysApply: true
---

# 全新开发原则（强制）

本仓库当前主产品是 **`desktop/`**（.NET + Vue）。`cli/` **只作功能结果参考**，不是运行时依赖，也不是迁移过渡层。

## 必须

1. **一步到位用新架构**：安装 / 卸载 / 清单 / IPC / UI 都在 `desktop/` 内设计与实现。
2. **参考旧能力时**：只对照「要达到什么结果」（装什么、配置什么、验收标准），**重写**实现，不要包一层旧脚本。
3. **新代码命名与注释**：按桌面产品表述（ToolCatalog、Installer、Job），不要出现「过渡期 / legacy / 临时调 cli」作为正式设计。

## 禁止

1. 运行时去执行 `cli/package/tools/**` 下的交互式 TUI / 旧入口脚本当正式安装路径。
2. 为「兼容旧 CLI」加双轨 API、双份配置、环境变量指回 `cli` 当主路径。
3. 先写临时胶水再打算以后重构——直接按最终形态写。

## 与 `cli/` 的关系

| `cli/` | 用途 |
|--------|------|
| 可读 | 弄清某工具以前怎么装、装到哪、依赖什么 |
| 不可用 | 当 Desktop Job 的正式后端 |

## 自检

- [ ] 这次改动是否仍依赖 `cli/` 路径才能跑通主流程？若是 → 改掉。
- [ ] 是否用「过渡 / legacy」给正式 API 命名？若是 → 改成桌面域模型名。
